export enum UiVariant {
  DEFAULT = 'default',
  OUTLINE = 'outline'
}

export enum UiColor {
  PRIMARY = 'primary',
  WHITE = 'white',
  BLACK = 'black'
}

export enum UiSize {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large'
}

export enum UiFlair {
  SOFT = 'soft',
  ROUNDED = 'rounded',
  SQUARED = 'squared'
}

export enum UiTag {
  BUTTON = 'button',
  ROUTER = 'router-link',
  A = 'a',
  DIV = 'div'
}